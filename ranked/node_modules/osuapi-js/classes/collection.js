class Collection extends Map {
    array() {
        const arr = [];
        for (const value of this.values()) {
            arr.push(value);
        }
        return arr;
    }
}

module.exports = Collection;