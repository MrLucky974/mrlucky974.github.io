const osuAPI    = require('./index');
const readline  = require('readline').createInterface(process.stdin, process.stdout);

readline.question('Enter your osu! API key (you can get one from https://osu.ppy.sh/p/api):', async(key) => {
    const api = new osuAPI(key);
    console.log(await api.getUser('nrabulinski', 'standard', 4));
});